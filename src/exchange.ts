// A repeatable currency exchange, not a one-time purchase like the other
// shops — there's no "owned" state, just cash spent for tokens at a fixed
// rate, as many times as affordable.
export const CASH_EXCHANGE_COST = 10;
export const CASH_EXCHANGE_TOKENS = 5;
